dependencies:
  pre:
    - gem update --system
    - gem install bundler
test:
  override:
    - bundle exec rake ci
deployment:
  staging:
    branch: master
    heroku:
      appname: agile-alliance-brazil-identity
