dependencies:
  pre:
    - gem update --system
    - gem uninstall bundler -a -x
    - gem install bundler -v 1.16.0
test:
  override:
    - bundle exec rake ci
deployment:
  staging:
    branch: master
    heroku:
      appname: agile-alliance-brazil-identity
