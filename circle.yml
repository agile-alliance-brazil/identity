dependencies:
  pre:
    - gem update --system
    - gem install bundler
    - bundler check --path=vendor/bundle || bundler install --path=vendor/bundle --jobs=4 --retry=3
test:
  override:
    - bundler exec rake ci
deployment:
  staging:
    branch: master
    heroku:
      appname: agile-alliance-brazil-identity
