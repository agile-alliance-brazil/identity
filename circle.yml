dependencies:
  pre:
    - gem update --system
    - gem install bundler -v 1.16.0 # https://github.com/rubygems/rubygems/issues/2055
test:
  override:
    - bundle exec rake ci
deployment:
  staging:
    branch: master
    heroku:
      appname: agile-alliance-brazil-identity
