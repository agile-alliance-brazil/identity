.sign_up
  %h2= t('activerecord.models.users.new')

  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = render partial: 'form', locals: {f: f, submit_text: t('activerecord.models.users.new')}
